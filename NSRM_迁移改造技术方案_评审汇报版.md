# NSRM V3.84.0 迁移改造技术方案 — 评审汇报版

> 配套详版：`NSRM_迁移改造技术方案.md`（评审稿 v2.0）
> 适用：架构评审 / 领导决策

---

## 一、背景与痛点
- 当前代码库**双栈并存**：旧栈 `ZT.SRM.*`（.NET Framework 4.5.2，已 EOL）+ 新栈 `NSRM_Core`（.NET 5，已建，覆盖大部分业务域，含 `ApiExternal` 移动出口）。
- 旧栈框架已停止支持，存在安全/合规风险；但旧系统业务逻辑庞大，全量重写成本过高。
- 移动端/H5、AuthCenter JWT、现代内部包要求 netstandard2.0+，旧 net452 支持极差；新栈 `NSRM_Core` 已是 ASP.NET Core，应**原地升 net10** 作统一目标。

## 二、核心决策（已确认）
| 项 | 决策 |
|---|---|
| 目标框架 | **.NET 10 LTS**（应用于 `NSRM_Core`；旧 `ZT.SRM` 维持 net452 维护，**不升级**） |
| 改造形态 | **不新建框架**：`NSRM_Core` 原地升 net10；旧系统进入**维护态由 AI 全托管**；仅大改模块迁入 `NSRM_Core` 重写，**新接口在 NSRM_Core 实现** |
| 切割方式 | **独立子域名 + 外部菜单/角色权限**，按菜单路径灰度，可回滚 |
| 登录校验 | `NSRM_Core` 沿用既有 JWT/AuthCenter；旧系统维护期保留旧 `ZT.FCL.Authentication` |
| 移动端 | 形态定为 **H5（可封装 App）**，复用 `ApiExternal`(net10) 出口，前端在 `NSRM_Core` 对接 |
| 数据层 | 迁入模块用 **SqlSugar**；旧系统维持 EF6 不动 |

## 三、总体策略
> 旧 `ZT.SRM`(net452) 与 新 `NSRM_Core`(net10) 是**两套独立部署**的应用，不混编框架、不互相引用，仅通过**同一数据库**与**外部菜单/子域名**关联——结构上隔离了"人/AI 改同一处"与"发布时间不一致"的冲突。旧系统 AI 全托管维护；大改模块迁入 `NSRM_Core` 用新框架重写。

| 维度 | 旧系统 `ZT.SRM`(net452) | 目标 `NSRM_Core`(net5→net10) |
|---|---|---|
| 组成 | MVC + EF6 + 旧认证 | `Api` + `ApiExternal`(移动出口) + `Services`(SqlSugar) + H5/Vue3 |
| 定位 | **维护态**：AI 全托管，修 bug/小改，不升框架 | 新业务/移动接入面；大改模块迁入重写；新接口在此实现 |
| 是否改框架 | 否 | 是：net5 → net10（原地，低成本） |

- 入口切割：外部菜单按角色导向旧/新子域；共享基础设施：同一数据库、Apollo、Redis、MongoDB、AuthCenter SSO。

## 四、分阶段实施
| 阶段 | 内容 |
|---|---|
| **P0 地基/验证** | 内部包 net10 资产验证；`NSRM_Core` 原地升 net10 骨架；AuthCenter 互通；外部菜单切割配置 |
| **P1 AI 维护机制** | 旧系统代码地图梳理；AI 全托管维护分支/PR 流程落地 |
| **P2 模块迁入** | 大改模块在 `NSRM_Core` 实现（Service+SqlSugar+DTO+前端），外部菜单切试点角色，可回滚 |
| **P3 前端** | H5/Vue3 页面迁移，未迁页反向代理共存 |
| **P4 退役** | 旧栈流量归零后归档下线 |

## 五、可行性论证（动工前置 Go/No-Go，6 项 PoC 须全过）
1. 内部包 net10 资产验证（**最高优先级阻塞项**）
2. `NSRM_Core` 升 net10 骨架跑通 + 既有 JWT/AuthCenter 校验正常
3. SqlSugar 同库只读共存（EF6 ↔ SqlSugar）
4. 子域 + 外部菜单 + 角色灰度/回滚 PoC
5. H5 + ApiExternal + Bearer 形态 PoC
6. Quartz 双版本共存/隔离验证

## 六、AI 接手与冲突管控（核心）
- **两类边界**：旧系统维护（AI 全托管，net452 内改）= 与 `NSRM_Core` 升级**独立部署、互不编译冲突、发布节奏独立**。
- **模块锁 + 分支/PR + 统一发布窗口**：任一模块单一所有者，AI 改动走 hotfix/feature 分支 → 人工 review → 合并，不直推主干。
- **共享核心先稳**：`ApiExternal`/AuthCenter/公共基类先夯实冻结，各迁入模块只消费不改共享文件。
- **质量闸门**：旧系统补表征测试 + 小步 PR + 人工 review；迁入模块编译+测试全绿 + 菜单路由回滚。

## 七、主要风险与对策
| 风险 | 对策 |
|---|---|
| 内部 `ZT.Core.*` / AuthCenter SDK 无 net10 资产 | P0 验证；若无需包 owner 重发 |
| 旧系统 AI 改出回归（无测试） | 表征测试 + 小步 PR + 人工 review |
| 同库双栈写冲突 | 维护期旧栈主写、迁入模块按表归属；Schema 变更统一评审 |
| Quartz 3.3↔3.8 跨组件通信异常 | 灰度期统一大版本或隔离调度器 |

## 八、待确认项
- `NSRM_Core` 升 .NET 10 还是先 net8（建议 10 LTS）
- SPA/移动端从门户获取 Bearer token 的交付方式
- 旧系统**模块状态看板**与**代码地图**初始化（高频维护模块优先）
- 域覆盖缺口分析（旧 `BLL/APIBLL` vs `NSRM_Core` `Services`）

## 九、预期收益
- 旧系统 AI 全托管，释放人力；新能力/移动端统一走 `NSRM_Core`(net10)，消除 EOL 风险；
- 两套独立部署，灰度/回滚互不影响，业务无感；
- 统一 Web/H5/移动端 API 与鉴权，支撑移动端接入；
- 借升级清除 `SmartThreadPool`、`DinkToPdf` 原生依赖等遗留技术债。

---
*详版与全部破坏性变更、依赖矩阵、AI 管控细则见配套技术方案文档（v2.0）。*
